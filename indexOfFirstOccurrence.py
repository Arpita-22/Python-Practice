string = "ababababa"

index = string.find("aba")

string2 = "welcome!"

#-1 means did not find any index, prints the index corresponding to the first letter of the substring in the string
index2 = string2.find("com")

print(index2)